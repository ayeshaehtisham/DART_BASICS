 // Write a Dart program to calculate and print the area of a circle given its radius.
 
 void main() {
  double radius = 2.5;
  double Result = area(radius); 
  print("Area of Radius: $radius is $Result");
 }

  double area (Radius) {
  double formula = (3.142) * Radius * Radius;
   return formula;
 }